import { ASTVisitorKeyMap } from './../../../node_modules/@graphql-tools/utils/typings/types.d';
export enum StatusEnum {
  PENDING = 'pending',
  CONFIRM = 'confirm',
  SHIPPED = 'shipped',
  DELIVERED = 'delivered',
  CANCELED = 'canceled',
  RETURNED = 'returned',
  ASKED = 'asked',
}
