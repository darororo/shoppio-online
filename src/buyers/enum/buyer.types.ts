import { CacheControlScope } from './../../../node_modules/@apollo/server/src/plugin/schemaReporting/generated/operations.d';
export enum SourceEnum {
  PURCHASE = 'purchased',
  ASKING = 'asking',
  OTHER = 'other',
  Delivered = 'delivered',
  CANCELLED = 'cancelled',
}
